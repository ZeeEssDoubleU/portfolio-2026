# Optional canonical origin; no secrets are required to build.
NEXT_PUBLIC_SITE_URL=https://zsw-portfolio-2026.netlify.app
# Optional, read-only CMS token used only by yarn sync:cms. Never prefix this with NEXT_PUBLIC_.
# DATO_API_TOKEN=
